1. The following hosts are under the secret `{{ .Release.Name }}-secret`

  {{- range .Values.hosts }}
    - {{ . }}
  {{- end }}

2. Add the `tls[0].secretName` to your Ingresses

  ```
  tls:
    - secretName: {{ .Release.Name }}-secret
  ```

3. More

  - `kubectl describe cert/{{ include "certificate.fullname" . }} --namespace {{ .Release.Namespace }}`
